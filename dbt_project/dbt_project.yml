name: 'ridepulse_dbt'
version: '1.0.0'
config-version: 2

profile: 'ridepulse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  ridepulse_dbt:
    staging:
      +schema: analytics
      +materialized: view
    analytics:
      +schema: analytics
      +materialized: view
